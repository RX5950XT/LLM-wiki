{
  "installCommand": "cd ../.. && bun install --frozen-lockfile",
  "buildCommand": "cd ../.. && bun run build --filter=@llm-wiki/web",
  "crons": [
    {
      "path": "/api/lint/cron",
      "schedule": "0 3 * * 1"
    }
  ]
}
